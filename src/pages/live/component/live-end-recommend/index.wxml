<view class="remind" wx:if="{{liveInfo.roomName}}" bind:tap="joinRoom">
  <view class="remind-name one-line">{{liveInfo.roomName}}直播预告</view>
  <view class="remind-bottom">
    <text class="remind-bottom-time">{{countdownText}}</text>
    <!-- <button class="remind-bottom-btn" bindtap="btnHandler">开播提醒</button> -->
    <!-- <button class="remind-btn remind-over" bindtap="btnHandler">已提醒</button> -->
  </view>
</view>